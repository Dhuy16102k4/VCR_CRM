<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Format_Phone</fullName>
    <active>true</active>
    <description>Phone number must start with 0, not 00, and have 10-11 digits.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Phone)),
  NOT(REGEX(Phone, &quot;^0[1-9][0-9]{8,9}$&quot;))
)</errorConditionFormula>
    <errorDisplayField>Phone</errorDisplayField>
    <errorMessage>Invalid phone number. Must start with 0 (not 00) and have 10-11 digits.</errorMessage>
</ValidationRule>
